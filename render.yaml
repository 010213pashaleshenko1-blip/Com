# Render.com configuration
services:
  - type: web
    name: openai-api-checker
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
